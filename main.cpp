#include<bits/stdc++.h>

using namespace std;

int main()
{
    class BigInt{
        private:
            string digits;

        public:
            BigInt(unsigned long long n = 0);
    
            BigInt(string s);
    };
}
